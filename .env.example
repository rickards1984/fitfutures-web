# Backend API base URL
VITE_API_URL=http://localhost:8000

# Supabase (frontend uses the anon/public key only)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Web push (VAPID public key — safe to expose)
VITE_VAPID_PUBLIC_KEY=
